<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" android:layout_gravity="center" android:paddingLeft="20dp" android:paddingRight="20dp" android:paddingTop="10dp" android:paddingBottom="20dp"
    android:background="@drawable/leather"
    >
    	<ImageView
    	    android:id="@+id/title_view"
    	    android:layout_width="fill_parent" 
            android:layout_height="wrap_content" 
            android:src="@drawable/title"></ImageView>
    	
    	<ImageView 
    	    android:layout_width="fill_parent"
			android:layout_height="wrap_content" 
			android:layout_below="@+id/title_view"
			android:src="@drawable/chips" 
			/>
    	
    	<Button
    	    android:id="@+id/edit_button_main_activity"
    	    android:layout_width="fill_parent"
    	    android:layout_height="wrap_content"
    	    android:layout_marginBottom="10dp" 
    	    android:layout_alignParentBottom="true"
    	    android:textColor="#ffffff"
    	    android:text="@string/edit_tournament_button"
    	    android:background="@drawable/poker_button_style"/>
    	
    	<Button
    	    android:id="@+id/start_button_main_activity"
    	    android:layout_width="fill_parent"
    	    android:layout_height="wrap_content"
    	    android:layout_marginBottom="10dp"
    	    android:layout_above="@+id/edit_button_main_activity"
    	    android:textColor="#ffffff"
    	    android:text="@string/start_tournament_button"
    	    android:background="@drawable/poker_button_style"/>
</RelativeLayout>